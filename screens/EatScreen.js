import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import tw from 'twrnc'

const EatScreen = () => {
  return (
    <View
    style={tw`bg-white h-full flex justify-center items-center p-5`}
    >
      <Text
      style={tw`text-lg text-center`}
      >Already implemented food panda clone, no need to do it here. Do check it out!</Text>
    </View>
  )
}

export default EatScreen

const styles = StyleSheet.create({})